#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;

    if(a >= 0 && a <= 100) {
        cout << "between 0 and 100\n";
    } else {
        cout << "Error\n";
    }
}