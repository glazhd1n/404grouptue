#include <bits/stdc++.h>

using namespace std;

int main() {
    float a;
    cin >> a;

    if(sqrt(a) == int(sqrt(a))) {
        cout << "Perfecto\n";
    } else {
        cout << "Simple\n";
    }
    
}